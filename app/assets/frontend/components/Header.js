import React, {PropTypes} from 'react';
// import {Link, IndexLink } from 'react-router';

const Header = () => {
  return (
  <div className="container header">
    <h1>Book with UrbanYou</h1>
  </div>
  );
};


Header.propTypes = {
};

export default Header;
